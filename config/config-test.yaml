# 测试环境配置
app:
  environment: "test"
  port: 8081

database:
  type: "sqlite"
  name: "resource_share_site_test"

redis:
  host: "localhost"
  port: "6379"
  db: 1  # 使用不同的数据库

log:
  level: "info"
  output: "file"
  filename: "logs/test.log"
